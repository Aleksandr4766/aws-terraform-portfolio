# Ігноруємо службові файли Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Ігноруємо файли з реальними паролями та ключами
*.tfvars
*.tfvars.json

# Ігноруємо секрети AWS (якщо раптом вони там є)
.aws/